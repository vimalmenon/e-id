import { AppLayout } from "../Layout";
import { metadata } from "../data";

const RegisterEmployee = () => {
  return (
    <AppLayout metadata={metadata.RegisterEmployee}>
      <div>This is register Employee</div>
    </AppLayout>
  );
};

export default RegisterEmployee;
