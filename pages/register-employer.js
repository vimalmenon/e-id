import { AppLayout } from "../Layout";
import { metadata } from "../data";

const RegisterEmployer = () => {
  return (
    <AppLayout metadata={metadata.RegisterEmployee}>
      <div>This is register Employer</div>
    </AppLayout>
  );
};

export default RegisterEmployer;
